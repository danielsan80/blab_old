{% extends 'DanPluginDiaryBundle::layout.html.twig' %}

{% block title %}blab{% endblock %}
{% block content %}
    <h2>Tmp</h2>
    <div class="row">
        <div class="span6">
            <h3>Form</h3>
            {{ form(form_data) }}
            <h3>Data</h3>
            <pre>{{yaml}}</pre>
        </div>
        <div class="span6">
            <h3>Def</h3>
            {{ form_start(form_def) }}
                {{ form_errors(form_def) }}
                {{ form_widget(form_def.data) }}

                {{ form_errors(form_def.data) }}
            {{ form_end(form_def) }}
        </div>
    </div>
    
{% endblock %}
