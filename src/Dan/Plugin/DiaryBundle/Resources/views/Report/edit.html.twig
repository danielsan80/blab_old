{% extends 'DanPluginDiaryBundle::layout.html.twig' %}

{% block title %}blab{% endblock %}
{% block content %}
    <div class="pull-right">
        <ul class="unstyled inline record_actions">
            <li>
                {{ form_start(delete_form) }}
                    {{ form_widget(delete_form.submit, {attr: {class:"btn btn-mini btn-danger"}}) }}
                {{ form_end(delete_form) }}
            </li>
            <li>
                <a href="{{ path('report') }}" class="btn btn-mini">
                    List
                </a>
            </li>
        </ul>
    </div>
    <h3>Report</h3>

    <div class="row">
        <div class="span6">
            {% include 'DanPluginDiaryBundle:Report:_form.html.twig' with {form: edit_form} %}
        </div>
        <div class="span6">
        </div>
    </div>

{% endblock %}
